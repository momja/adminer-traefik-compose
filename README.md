# How do I configure the password?
Set a new password hash using PHP in plugins-enabled/login-password-less.php

# Why is there a password set by adminer?
Sqlite does not support encrypted databases. This provides a bypass for adminer to support sqlite without removing it's authentication layer
